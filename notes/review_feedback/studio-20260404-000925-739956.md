# Review Feedback studio-20260404-000925-739956

- Round: 1
- Brief: Low-prep writing activities that help English students think faster

## Winner
- Variant: 2
- Style: Light Grain Glow
- Copy: Expanded
- Figma: n/a
- Winner note: this is only the winner because it has the least ammount of issues The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. This issue does not appear here. Even though the slide includes an image, the text is not overflowing.
- Slide refs:
  - /slide_2: /studio-output/previews/studio-20260404-000925-739956-v2/slide-02.png
  - /slide_3: /studio-output/previews/studio-20260404-000925-739956-v2/slide-03.png
  - /slide_5: /studio-output/previews/studio-20260404-000925-739956-v2/slide-05.png

## Rejected Variants
- Variant 1: Placeholder Media Glow / Balanced
  Feedback: The text is overflowing off the page, so part of it is not visible. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read.
  Slide refs:
    - /slide_1: /studio-output/previews/studio-20260404-000925-739956-v1/slide-01.png
    - /slide_2: /studio-output/previews/studio-20260404-000925-739956-v1/slide-02.png
    - /slide_3: /studio-output/previews/studio-20260404-000925-739956-v1/slide-03.png
    - /slide_4: /studio-output/previews/studio-20260404-000925-739956-v1/slide-04.png
    - /slide_5: /studio-output/previews/studio-20260404-000925-739956-v1/slide-05.png
    - /slide_6: /studio-output/previews/studio-20260404-000925-739956-v1/slide-06.png
- Variant 3: Device Mockup Gradient / Tight
  Feedback: The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The “Follow for more” image or graphic is touching the bold text, which should not be happening.
  Slide refs:
    - /slide_2: /studio-output/previews/studio-20260404-000925-739956-v3/slide-02.png
    - /slide_3: /studio-output/previews/studio-20260404-000925-739956-v3/slide-03.png
    - /slide_4: /studio-output/previews/studio-20260404-000925-739956-v3/slide-04.png
    - /slide_5: /studio-output/previews/studio-20260404-000925-739956-v3/slide-05.png
    - /slide_6: /studio-output/previews/studio-20260404-000925-739956-v3/slide-06.png
    - /slide_7: /studio-output/previews/studio-20260404-000925-739956-v3/slide-07.png

## Action Notes
- Keep/refine from winner: this is only the winner because it has the least ammount of issues The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. This issue does not appear here. Even though the slide includes an image, the text is not overflowing.
- Problems to avoid: The text is overflowing off the page, so part of it is not visible. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read. The body text is touching the bold heading, which makes it hard to read. | The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The body text is touching the bold text, which makes it difficult to read. The overflow seems to happen only on slides that contain an image, so the image may be causing the issue. The “Follow for more” image or graphic is touching the bold text, which should not be happening.
